# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Local Claude Code settings — per-machine, not shared.
# settings.json (committed) holds shared project permissions;
# settings.local.json stays local.
.claude/settings.local.json

# Environment overrides (keep .env and .env.example tracked)
.env*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
